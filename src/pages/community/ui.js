// Shared visual constants for the Community tab and its extracted parts, kept in
// one place so CommunityTab.jsx and the split-out modal/list files stay in visual
// lockstep instead of each carrying their own copy.
export const CARD_STYLE = { background: 'var(--surface)', border: '0.5px solid var(--border)' };
export const SUBTLE_BTN = { background: 'var(--input-bg)', color: 'var(--text-2)', border: '0.5px solid var(--input-border)' };
export const INPUT_STYLE = { background: 'var(--input-bg)', border: '0.5px solid var(--input-border)', color: 'var(--text-1)' };
export const MODAL_INPUT_CLASS = 'w-full px-4 py-3 rounded-xl text-sm focus:outline-none mb-3';
